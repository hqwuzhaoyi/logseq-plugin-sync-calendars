{
  "name": "logseq-plugin-react-boilerplate",
  "private": true,
  "scripts": {
    "build": "dotenv -- turbo build",
    "dev": "dotenv -- turbo dev --summarize",
    "lint": "dotenv -- turbo lint",
    "release": "dotenv -- turbo release",
    "build:plugin": "dotenv -- turbo build:plugin",
    "build:web": "dotenv -- turbo build:web",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "turbo": "^2.0.9"
  },
  "packageManager": "pnpm@8.15.6"
}